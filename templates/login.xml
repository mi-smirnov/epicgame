<fest:template xmlns:fest="http://fest.mail.ru"
    context_name="json">
    <div class="logoText">Tanks</div>
    <p id="titleText">Login</p>
        <div class= "menu">
            <legend>Sign In</legend>
            <form method= "POST" action="/api/v1/auth/signin" id="login">
                <input type= "text" class="span4" placeholder="email" name="email"/>
                <input type= "password" class="span4" placeholder="password" name="password"/>
                <div class="logSubmit"><button type="submit" name="submit" class="btn btn-block btn-primary">Sign in</button></div>
            </form>
        </div>  
    <a href="#"><button class="btn js-back" type="button"><i class="icon-arrow-left"></i> Back</button></a>
</fest:template>